const clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,n));
const phone=document.querySelector('.phone'),copy=document.querySelector('.hero-copy'),dash=document.querySelector('.dashboard'),map=document.querySelector('.display .map'),full=document.querySelector('.map-full'),route=document.querySelector('.route'),marker=document.querySelector('.boat-marker'),limit=document.querySelector('.limit'),bar=document.querySelector('.progress span');
function render(){const y=scrollY,h=innerHeight,max=document.documentElement.scrollHeight-h,p=clamp(y/max);bar.style.height=`${p*100}%`;const enter=clamp((y-h*.15)/(h*1.55));copy.style.opacity=1-clamp(enter*1.25);phone.style.transform=`perspective(1200px) translate3d(${6-6*enter}vw,${18-18*enter}vh,${enter*380}px) rotateY(${-17+17*enter}deg) rotateX(${6-6*enter}deg) scale(${.48+.5*enter})`;const morph=clamp((y-h*1.35)/(h*1.25));dash.style.opacity=1-morph;map.style.opacity=morph;map.style.transform=`scale(${1.12-.12*morph})`;const breakout=clamp((y-h*2.15)/(h*1.4));full.style.opacity=breakout;full.style.transform=`scale(${.22+.78*breakout})`;route.style.clipPath=`inset(0 ${100-clamp((breakout-.2)*2.1)*100}% 0 0)`;marker.style.opacity=clamp((breakout-.32)*3);marker.style.transform=`translate3d(0,${(1-clamp((breakout-.32)*3))*20}px,0)`;limit.style.opacity=clamp((breakout-.68)*4);requestAnimationFrame(render)}
requestAnimationFrame(render);
